namespace JDoc
{
    public enum ReferenceOption
    {
        /// <summary>
        /// Document references may only be to a revision of a document
        /// </summary>
        RevisionOnly,
    }
}